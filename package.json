{
  "name": "privateswap",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run compile -w packages/contracts && npm run build -w apps/web",
    "test": "npm run test -w packages/contracts",
    "compile": "npm run compile -w packages/contracts",
    "dev:web": "npm run dev -w apps/web"
  }
}
