# Deployer key (testnet only — never use a mainnet key)
PRIVATE_KEY=0x...

# Optional RPC (defaults to public Sepolia node in hardhat.config.ts)
SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com

# Optional Etherscan verification
ETHERSCAN_API_KEY=
